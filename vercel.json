{
    "framework": "nextjs",
    "installCommand": "npm install && npx prisma generate",
    "env": {
        "DATABASE_URL": "file:./placeholder.db"
    },
    "crons": [
        {
            "path": "/api/cron/update",
            "schedule": "0 0 * * *"
        }
    ]
}